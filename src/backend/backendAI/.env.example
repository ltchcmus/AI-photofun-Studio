# Environment variables for backendAI# Environment variables for development

# Copy this to .env and fill with your actual valuesDEBUG=True

USE_SQLITE=True

# Django

DEBUG=True# Database (PostgreSQL)

SECRET_KEY=django-insecure-change-this-in-productionDB_NAME=backendai_db

ALLOWED_HOSTS=localhost,127.0.0.1DB_USER=postgres

DB_PASSWORD=postgres

# Database (PostgreSQL)DB_HOST=localhost

USE_SQLITE=TrueDB_PORT=5432

DB_NAME=backendai_db

DB_USER=postgres# Secret Key (generate a new one for production)

DB_PASSWORD=postgresSECRET_KEY=django-insecure-change-this-in-production

DB_HOST=localhost

DB_PORT=5432# AI/ML Configuration

USE_GPU=False

# Celery & RedisGPU_DEVICE_ID=0

CELERY_BROKER_URL=redis://localhost:6379/0

CELERY_RESULT_BACKEND=redis://localhost:6379/0# Celery & Redis

REDIS_URL=redis://localhost:6379/1CELERY_BROKER_URL=redis://localhost:6379/0

CELERY_RESULT_BACKEND=redis://localhost:6379/0

# MongoDB Configuration (for conversation service)REDIS_URL=redis://localhost:6379/1

MONGO_URI=mongodb://localhost:27017

MONGO_DB_NAME=ai_photofun_studio# AWS S3 (optional)

# AWS_ACCESS_KEY_ID=your_access_key

# AI/ML Configuration# AWS_SECRET_ACCESS_KEY=your_secret_key

USE_GPU=False# AWS_STORAGE_BUCKET_NAME=your_bucket_name

GPU_DEVICE_ID=0# AWS_S3_REGION_NAME=us-east-1



# CORS Origins (comma-separated)# Sentry (optional - for error tracking)

CORS_ORIGINS=http://localhost:3000,http://localhost:5173,http://localhost:9999# SENTRY_DSN=your_sentry_dsn



# AWS S3 (optional)# CORS Origins (comma-separated)

# AWS_ACCESS_KEY_ID=your_access_keyCORS_ORIGINS=http://localhost:3000,http://localhost:5173

# AWS_SECRET_ACCESS_KEY=your_secret_key

# AWS_STORAGE_BUCKET_NAME=your_bucket_name# MongoDB (optional)

# AWS_S3_REGION_NAME=us-east-1# You can provide a full MONGO_URI or individual components below.

# Example full URI (Atlas): mongodb+srv://user:pass@cluster0.xxxxxx.mongodb.net/mydb?retryWrites=true&w=majority

# Sentry (optional - for error tracking)MONGO_URI=

# SENTRY_DSN=your_sentry_dsnMONGO_HOST=localhost

MONGO_PORT=27017
MONGO_USER=
MONGO_PASSWORD=
MONGO_DB=backend_ai
MONGO_OPTIONS=
