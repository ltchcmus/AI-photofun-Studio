from django.apps import AppConfig


class BackgroundRemovalConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.background_removal'
    verbose_name = 'Background Removal'
